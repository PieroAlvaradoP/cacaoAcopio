<form method="post" action="{{ path('app_empresa_transporte_delete', {'id': empresa_transporte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ empresa_transporte.id) }}">
    <button class="btn">Delete</button>
</form>
