<form method="post" action="{{ path('app_guia_remision_delete', {'id': guia_remision.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ guia_remision.id) }}">
    <button class="btn">Delete</button>
</form>
