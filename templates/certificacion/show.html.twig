{% extends 'base.html.twig' %}

{% block title %}Certificacion{% endblock %}

{% block body %}
    <h1>Certificacion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ certificacion.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ certificacion.nombre }}</td>
            </tr>
            <tr>
                <th>Alias</th>
                <td>{{ certificacion.alias }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ certificacion.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('certificacion_index') }}">back to list</a>

    <a href="{{ path('certificacion_edit', {'id': certificacion.id}) }}">edit</a>

    {{ include('certificacion/_delete_form.html.twig') }}
{% endblock %}
