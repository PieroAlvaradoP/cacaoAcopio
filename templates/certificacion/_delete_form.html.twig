<form method="post" action="{{ path('certificacion_delete', {'id': certificacion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ certificacion.id) }}">
    <button class="btn">Delete</button>
</form>
