{% extends 'base.html.twig' %}

{% block title %}DocumentoTramite{% endblock %}

{% block body %}
    <h1>DocumentoTramite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ documento_tramite.id }}</td>
            </tr>
            <tr>
                <th>Serie</th>
                <td>{{ documento_tramite.serie }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ documento_tramite.numero }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_documento_tramite_index') }}">back to list</a>

    <a href="{{ path('app_documento_tramite_edit', {'id': documento_tramite.id}) }}">edit</a>

    {{ include('documento_tramite/_delete_form.html.twig') }}
{% endblock %}
