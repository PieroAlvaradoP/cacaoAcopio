<form method="post" action="{{ path('app_marca_vehiculo_delete', {'id': marca_vehiculo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ marca_vehiculo.id) }}">
    <button class="btn">Delete</button>
</form>
