<form method="post" action="{{ path('motivo_traslado_delete', {'id': motivo_traslado.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ motivo_traslado.id) }}">
    <button class="btn">Delete</button>
</form>
